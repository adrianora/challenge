def searchMin(array):
        min = array[0]
            for element in range(0, len(array)):
                        if element < min
                                    min = element
                                        return min
