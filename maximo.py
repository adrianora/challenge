def searchMax(array):
        max = array[0]
            for element in range(0, len(array)):
                        if element > max
                                    max = element
                                        return max
